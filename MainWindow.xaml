<Window x:Class="RainmeterLayoutManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RainmeterLayoutManager" d:DataContext="{d:DesignInstance Type=local:LayoutMappingViewModel}"
        mc:Ignorable="d"
        Title="Rainmeter Layout Manager" Height="550" Width="800" MinWidth="600" MinHeight="400">
    <Grid Margin="15" MouseDown="Grid_MouseDown" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Section 1: Current Display Info -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <StackPanel>
                    <TextBlock Text="Current Display Fingerprint" FontWeight="SemiBold"/>
                    <TextBlock x:Name="FingerprintText" Text="Loading..." Foreground="Gray" TextWrapping="Wrap" Margin="0,5,0,0" FontSize="11"/>
                </StackPanel>
                <Button x:Name="RefreshFingerprintButton" 
                        Click="RefreshFingerprint_Click"
                        ToolTip="Refresh display fingerprint"
                        Width="28" Height="28" 
                        Margin="10,0,0,0"
                        VerticalAlignment="Top"
                        Background="Transparent" 
                        BorderThickness="0">
                    <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                </Button>
            </StackPanel>

            <CheckBox x:Name="StartWithWindowsCheckBox" Grid.Column="2" Content="Start with Windows" 
                      VerticalAlignment="Center" HorizontalAlignment="Right"
                      Click="StartWithWindowsCheckBox_Click" IsChecked="False"/>
        </Grid>
        
        <!-- Section 2: Add Layout Binding -->
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <Button x:Name="AddLayoutBindingButton" 
                    Content="Add Layout Binding" 
                    Width="Auto"
                    Padding="10,5"
                    Click="AddLayoutBinding_Click"/>
        </StackPanel>

        <!-- Section 3: Mapped Layouts -->
        <GroupBox Grid.Row="3" Header="Saved Layout Mappings">
            <DataGrid x:Name="MappingsGrid" AutoGenerateColumns="False" CanUserAddRows="False"
                      ItemsSource="{Binding LayoutMappings}" CellEditEnding="MappingsGrid_CellEditEnding"
                      GridLinesVisibility="None" BorderThickness="0"
                      HeadersVisibility="Column" RowHeaderWidth="0"
                      SelectionMode="Single" SelectionUnit="FullRow"
                      CanUserResizeRows="False" AlternationCount="{Binding MaxLayoutMappings}">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="BorderThickness" Value="0,1,0,0"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Style.Triggers>
                            <!-- Remove top border from first row -->
                            <Trigger Property="AlternationIndex" Value="0">
                                <Setter Property="BorderThickness" Value="0"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#DDDDDD"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F0F0F0"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="8,5"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#DDDDDD"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Display Fingerprint" Binding="{Binding Fingerprint}" IsReadOnly="True" Width="*"/>
                    <DataGridTextColumn Header="Rainmeter Layout" Binding="{Binding LayoutName, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" Width="*"/>
                    <DataGridTemplateColumn Width="Auto" Header="Actions" CanUserResize="False" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Click="EditMapping_Click" ToolTip="Edit mapping" 
                                            Width="28" Height="28" Margin="2" 
                                            Background="Transparent" BorderThickness="0">
                                        <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                                    </Button>
                                    <Button Click="RemoveMapping_Click" ToolTip="Remove mapping"
                                            Width="28" Height="28" Margin="2"
                                            Background="Transparent" BorderThickness="0">
                                        <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
        
    </Grid>
</Window>
